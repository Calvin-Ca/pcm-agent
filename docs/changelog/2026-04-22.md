# 2026-04-22

## E2E 修复（5 个 P1 阻断问题）

本次修复让 ai-service E2E 链路彻底跑通。架构结论：反向 SSH 隧道（已运行，保留）+ SpringBoot AIController 做网关（不改 nginx 直连）+ docker-compose 不拆（修好 override 就够了）。

### 变更清单

| # | 问题 | 改动位置 | 状态 |
|---|------|----------|------|
| P1-1 | docker-compose REDIS_PORT=16379 覆盖 .env | `docker-compose.yml:125` `16379→6379` | ✅ 已改 |
| P1-2 | docker-compose SPRINGBOOT_BASE_URL 指向 host.docker.internal:9900（172 上无 SpringBoot） | `docker-compose.yml:129` 删除该行，让 .env 生效 | ✅ 已改 |
| P1-3 | AIController 4 处 URL 缺 `/api` 前缀 | `springboot3/.../AIController.java` | ✅ 已改，需重新部署 |
| P1-4 | SpringBoot AI_SERVICE_URL 未指向反向隧道端口 | 116 `gongshi-ht.sh` 启动脚本 | ✅ 用户已添加 |
| P1-5 | 172 .env 缺 DASHSCOPE_API_KEY | — | ❌ 不需要（本地 vLLM 做 embedding） |

### P2 改动（功能降级）

| # | 问题 | 改动位置 | 状态 |
|---|------|----------|------|
| P2-1 | AIController 不透传 Authorization header | `springboot3/.../AIController.java` chat() | ✅ 已改，需重新部署 |
| P2-2 | entity_type/department_id 硬编码 USER/null | `AIController.java` + `AIPermissionInterceptor.java` | ✅ 已改，需重新部署 |
| P2-3 | nginx `/ai/` location 缺少 proxy_buffering off | 116 nginx 配置 | ✅ 已确认有（无需修改） |

### AIController.java 改动详情

| 行 | 原来 | 改为 |
|----|------|------|
| 109 | `aiServiceUrl + "/ai/chat/stream"` | `aiServiceUrl + "/api/ai/chat/stream"` |
| 203 | `aiServiceUrl + "/health"` | `aiServiceUrl + "/api/ai/health"` |
| 245 | `aiServiceUrl + "/tools"` | `aiServiceUrl + "/api/ai/status"` |
| 293 | `aiServiceUrl + "/tools/register"` | 直接返回 501（功能未实现） |

### P2-1 & P2-2 AIController.java + AIPermissionInterceptor.java 改动

**P2-1**：chat() 方法透传 Authorization header 给 ai-service
```java
// 行 112-113
.header(HttpHeaders.AUTHORIZATION, authorization)  // 新增
```

**P2-2**：从 JWT 读取真实 entityType（ENTITY_TYPE_KEY）和 departmentId（org_id），不再硬编码
- AIPermissionInterceptor：不再硬编码 `"USER"` / `null`，从 JWT 解码取 `entity_type` 和 `org_id` 写入 request attribute
- AIController.buildPermissionContext()：改签名接收 `entityType` 和 `departmentId` 参数，优先用 interceptor 设的真实值

### deploy-guide.md 清理

- §4.5 nginx：`proxy_pass http://172.19.3.136:8000` → `http://127.0.0.1:9900`（走 SpringBoot 网关），加隧道拓扑说明
- §4.4 拆分为 4.4a（Docker Compose，REDIS_HOST=redis/6379）和 4.4b（裸跑+Docker，REDIS_HOST=127.0.0.1/16379）
- §8.1：删掉 conda 裸跑推荐，保留 Docker Compose
- §8.2/8.3：删掉"推荐部署 116"段落，更新依赖连通性拓扑图（ai-service 在 172）
- §七：删掉重复的旧章节段落（ai-service/Docker、Milvus/生产、nginx、SpringBoot/参考）
- SQL_AGENT_DB_HOST：localhost → 192.168.0.94

### 待验证步骤（按顺序）

1. 容器层（172）：`docker compose up -d --force-recreate ai-service` → `docker exec ai-assistant-service sh -c 'echo $REDIS_PORT $SPRINGBOOT_BASE_URL'` 期望 `6379 https://gst.thsware.com`
2. 隧道层（116）：`curl -sf http://127.0.0.1:9901/api/ai/health`
3. SpringBoot 网关层（公网）：带 JWT 访问 `https://gst.thsware.com/api/ai/health`
4. E2E 工具调用：`curl -Nsk -H "Authorization: $TOKEN" -d '{"message":"查我本周工时","session_id":"smoke","stream":true}' https://gst.thsware.com/api/ai/chat`
5. E2E RAG 问答：`curl -Nsk -H "Authorization: $TOKEN" -d '{"message":"工时填报的截止时间","session_id":"smoke2","stream":true}' https://gst.thsware.com/api/ai/chat`

---

## 部署变更

### 生产环境部署架构调整

**变更内容**：
- ai-service + Redis + Milvus + MinIO 部署在 **172.19.3.136**（GPU 服务器），而非 116 服务器
- Spring Boot + nginx 仍部署在 **116.205.174.57**

### 端口映射更新（生产环境）

| 服务 | 宿主机端口 | 容器内部端口 |
|------|-----------|-------------|
| ai-service | 127.0.0.1:8000 | 8000 |
| Redis | 16379 | 6379 |
| Milvus | 29530 | 19530 |
| MinIO API | 29000 | 9000 |
| MinIO Console | 29001 | 9001 |

### 文档更新

- `docs/deployment.md` 架构拓扑图更新，标注 172/116 服务器分工
- 服务列表区分宿主机端口和容器内部端口
- 外部依赖说明更新
- 配置表新增 `DASHSCOPE_API_KEY` 说明（RAG Embedding 专用）
- `docker-compose.prod.yml` 添加 Redis 端口映射 `16379:6379`

### 服务状态

| 服务 | 状态 | 备注 |
|------|------|------|
| ai-assistant-service | healthy | FastAPI AI 核心服务 |
| ai-assistant-milvus | healthy | 知识库向量存储 |
| ai-assistant-redis | healthy | 会话记忆 |
| ai-assistant-minio | healthy | Milvus 对象存储 |

### 架构拓扑

```
172.19.3.136（GPU 服务器）
├── vLLM qwen3-8b :8099
├── vLLM bge-large :8097
├── Ollama :11434
└── Docker（ai-assistant-*）
    ├── ai-service :8000
    ├── redis :16379
    ├── milvus :29530
    └── minio :29000/29001

116.205.174.57（公网应用服务器）
├── Spring Boot :9900
└── nginx :80/:443
```
