# Function Calling vs 两步级联：统一单次推理 A/B 测试 Run 2

## 1. 测试配置

- 日期：2026-08-06
- 数据：项目当前完整单轮数据集，共2,000条
- 模型端点：DashScope OpenAI兼容API
- 模型：`qwen-plus`
- 并发：8
- A组：两步级联，一次被测请求内部执行“意图分类 → 参数提取”
- B组：一次Function Calling同时返回工具和参数
- 评测规则：每个方案、每条样本只调用一次完整架构，所有指标均从该次输出计算
- 检查点：每100条落盘

本Run使用云端`qwen-plus`，其结果仅代表本报告记录的模型、数据与运行环境，不能直接外推到生产vLLM。

## 2. 完整性校验

| 项目 | A组 | B组 |
|---|---:|---:|
| `complete` | `true` | `true` |
| 样本数 | 2,000 | 2,000 |
| 请求级错误 | 31 | 0 |
| 降级审计事件 | 0 | 0 |

A组31个错误均为：`AttributeError: 'list' object has no attribute 'strip'`，集中出现在多日期工时填报样本（`swhm_*`）。错误已按失败计入分母，不得从结果中剔除。该问题属于A组链路对列表型结果的兼容性问题，不是网络错误。具体原因是A组先执行意图分类，再由参数提取模型生成`save_workhour`参数；对于“今天和明天各填4小时”等多日期请求，参数提取结果中的`date`可能是日期列表，后续`node_classify_intent`将该值传给`_expand_multi_day_date()`，而当前函数按字符串处理`date_str`并直接调用`date_str.strip()`，因此收到列表时抛出异常并导致请求端到端失败。这31条是A组被测链路的真实运行时失败，不是统一评测脚本的参数评分异常。本Run仅记录原因，不修改业务实现；若后续修复列表型日期的校验与归一化，需要另起Run重新测试，不能回填或修正当前结果。

## 3. 准确率对比

| 指标 | A组两步级联 | B组Function Calling | B-A |
|---|---:|---:|---:|
| 意图准确率 | 1,779/2,000 = 88.95% | **1,845/2,000 = 92.25%** | **+3.30pp** |
| 路由联合准确率 | 1,468/2,000 = 73.40% | **1,742/2,000 = 87.10%** | **+13.70pp** |
| 工具端到端准确率 | 1,088/1,405 = 77.44% | **1,307/1,405 = 93.02%** | **+15.59pp** |
| 工具条件准确率 | 1,088/1,312 = 82.93% | **1,307/1,333 = 98.05%** | **+15.12pp** |
| 参数条件准确率 | 787/1,038 = 75.82% | **1,239/1,257 = 98.57%** | **+22.75pp** |
| 参数端到端准确率 | 787/1,355 = 58.08% | **1,239/1,355 = 91.44%** | **+33.36pp** |
| 联合端到端准确率 | 1,217/2,000 = 60.85% | **1,724/2,000 = 86.20%** | **+25.35pp** |
| 非工具误调用率 | 164/595 = 27.56% | **128/595 = 21.51%** | **-6.05pp** |
| 澄清准确率 | **108/149 = 72.48%** | 93/149 = 62.42% | -10.07pp |

说明：

- “条件准确率”只在前置路由正确的样本上评分，适合定位参数提取能力。
- “端到端准确率”将前置路由错误保留在分母中，更接近真实请求成功率。
- “联合端到端”要求同一次输出的意图、工具及适用参数全部通过。
- B组除澄清准确率外，其余主要准确率指标均优于A组。

## 4. 延迟对比

| 指标 | A组 | B组 | 变化 |
|---|---:|---:|---:|
| 单请求平均延迟 | 2.501秒 | **1.695秒** | **降低32.2%** |
| P50 | 2.816秒 | **1.584秒** | **降低43.7%** |
| P95 | 3.499秒 | **2.733秒** | **降低21.9%** |
| 最大延迟 | 8.235秒 | 8.234秒 | 基本相同 |
| 整组墙钟时间 | 654.44秒 | **442.86秒** | **降低32.3%** |

本Run的A、B均由统一脚本按一次完整架构调用计时，因此可以用于比较当前云端配置下的生产链路延迟。A组工具样本内部包含第二次模型调用，B组仅调用一次Function Calling。

## 5. 结论与边界

本次完整、同模型、同数据、统一口径的Run 2支持以下结论：

1. B组Function Calling的联合端到端准确率比A组高25.35个百分点。
2. B组参数端到端准确率比A组高33.36个百分点，是主要收益来源。
3. B组平均延迟比A组低32.2%，整组墙钟时间低32.3%。
4. B组没有请求级错误；A组存在31个列表型结果兼容错误。
5. B组澄清准确率低于A组10.07个百分点，需要单独优化，不应只报告优势指标。

边界：本Run使用云端`qwen-plus`，不能替代生产vLLM `qwen3-8b`的验证；目前仍缺少同配置Run 3以及多轮复杂对话测试，尚不能宣称复杂对话稳定性已经得到完整证明。

## 6. 原始结果

- `A_two_stage_unified_cloud_qwen_plus_run2.json`
- `B_function_calling_unified_cloud_qwen_plus_run2.json`
